Theorem (Factorial)
Given (x)
Proof
	suppose (x <= 1) perfomed 
	therefore proven 1;
	proven x * Factorial(x - 1); 
QED

y = 0;
Introduce(y);

Consider {
	z = Factorial(y); 
	y = y - 1; 
	Conclusion(z);
} assuming expression (y > 0) perfomed;
$
